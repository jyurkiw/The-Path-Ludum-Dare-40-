using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Mark the position where Tower Projectiles are supposed to spawn.
/// Simply used to find otherwise empty game objects.
/// </summary>
public class ProjectileSpawn : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
